# Supabase Policies

These SQL files contain Row-Level Security policies only.
Table structure is managed by Prisma migrations in `packages/database/prisma/migrations/`.
Apply these manually via the Supabase SQL editor or Supabase CLI after running `pnpm db:migrate`.
